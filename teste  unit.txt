%dw 2.0
output application/java
---
payload map((item, index) -> {
    "FS1_DataValidacao__c": 
        (
            // Convert to string and truncate to milliseconds
            (item.dat_rfrc as String)[0 to 26] ++ "Z" // Ensures we only take the first 26 characters and append 'Z' for UTC
        )
        as DateTime {format: "yyyy-MM-dd'T'HH:mm:ss.SSS'Z'"}
})
