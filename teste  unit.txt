%dw 2.0
output application/java
---
payload map((item, index) -> {
    "FS1_DataValidacao__c": 
        (
            // Ensure item.dat_rfrc is a valid string and format it
            (item.dat_rfrc as String)[0 to 26] ++ "Z" // Truncate to milliseconds and append 'Z'
        ) as DateTime {format: "yyyy-MM-dd'T'HH:mm:ss.SSS'Z'"}
})
